<%= form_with(model: personal_trainer, local: true) do |form| %>
  <% if personal_trainer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_trainer.errors.count, "error") %> prohibited this personal_trainer from being saved:</h2>

      <ul>
        <% personal_trainer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :firstName %>
    <%= form.text_field :firstName %>
  </div>

  <div class="field">
    <%= form.label :secondName %>
    <%= form.text_field :secondName %>
  </div>

  <div class="field">
    <%= form.label :desription %>
    <%= form.text_field :desription %>
  </div>

  <div class="field">
    <%= form.label :amountOfClients %>
    <%= form.text_field :amountOfClients %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
